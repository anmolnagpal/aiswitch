version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - misspell
    - unconvert
    - unparam
    - unused
  exclusions:
    rules:
      - path: "_test\\.go"
        linters: [unparam, errcheck]

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/anmolnagpal/aiswitch

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
